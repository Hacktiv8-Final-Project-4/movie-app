import React from "react";
import { MovieList } from "../components";
import Layout from "../components/Layout";

const Home = () => {
  return (
    <Layout>
      <MovieList />
    </Layout>
  );
};

export default Home;
