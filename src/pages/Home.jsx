import React from "react";
import { MainLayout, MovieList } from "../components";

const Home = () => {
  return (
    <MainLayout>
      <MovieList />
    </MainLayout>
  );
};

export default Home;
